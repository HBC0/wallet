```text
echo "# wallet" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:HBC0/wallet.git
git push -u origin main
```

```text
git remote add origin git@github.com:HBC0/wallet.git
git branch -M main
git push -u origin main
```# wallet
